{
  "name": "passbook-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:mobile": "npm run start --prefix mobile",
    "dev:all": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" \"npm run start --prefix mobile\"",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend && npm install --prefix mobile"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
